{
  "name": "sue-angels-fc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:app": "esbuild src/SiteApp.jsx --loader:.jsx=jsx --jsx=transform --jsx-factory=React.createElement --jsx-fragment=React.Fragment --charset=utf8 --legal-comments=none --outfile=SiteApp.js",
    "watch:app": "npm run build:app -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
