{
  "name": "sue-angels-fc",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Sue's Angels FC platform. Static output, generated locally and committed; Vercel needs no build step.",
  "scripts": {
    "build": "node src/build.mjs",
    "check": "node src/build.mjs --check",
    "verify": "node src/lib/verify-dataset.mjs",
    "test": "node src/test/run.mjs",
    "serve": "npx --yes serve@14 -l 4321 .",
    "pack": "node scripts/make-pack.mjs",
    "sync": "node scripts/sync.mjs",
    "publish": "npm run sync && npm run build && npm run verify && npm test",
    "covers": "node scripts/make-covers.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
