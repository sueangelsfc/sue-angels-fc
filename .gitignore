# OS / editor cruft
.DS_Store
**/.DS_Store
.vscode/
*.log

# Local tooling state (keep launch.json out of the repo)
.claude/launch.json

# Python (newsletter build)
__pycache__/
*.pyc
.venv/
venv/
.firecrawl/
node_modules/
.DS_Store
.vercel/
_sections.html
.vercel

# Scratch for scripts/make-covers.mjs. The script clears it on a clean run;
# a run that is interrupted leaves it behind, and it must never be committed.
.covers-tmp/

# Written only by `npm run visual`, and deleted again as soon as it has been
# rendered. Never a route: it is the home page with all 75 bands switched on.
__all-bands.html
